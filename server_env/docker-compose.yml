version: '3.8'

services:
  web:
    build: .
    container_name: aviation_classifier
    ports:
      - "127.0.0.1:5000:5000"
    volumes:
      # Пробрасываем обученную модель и маппинг в запущенный контейнер

      - ./classes_mapping.csv:/app/classes_mapping.csv
    restart: unless-stopped
